# Orchestration Log: Carl — Skip Provider Registration Review

**Timestamp:** 2026-05-07T11:36:00Z  
**Agent:** Carl (Lead / Architect)  
**Model:** haiku-4.5  
**Mode:** sync  
**Duration:** ~2 minutes  
**Result:** APPROVED

## Summary

Architectural review of Donut's `skip_provider_registration = true` hardcoding in Networking and Foundry-byoVnet.

## Verdict

REVERT + conditional variable:
1. Remove hardcoded `true` from both modules (revert to default `false`)
2. Add `variable "skip_provider_registration"` with `default = false` to both `variables.tf`
3. Update provider blocks to use variable reference
4. Document in README: set via `tfvars` only if account lacks provider registration RBAC

## Rationale

- Demo/lab environment should default to auto-register (simpler, more transparent)
- Conditional flag preserves solution for managed subscription RBAC constraints
- Documents intent explicitly — not a default config choice
- Networking LZ remains self-sufficient

## Written Decision

`.squad/decisions/inbox/carl-skip-provider-registration.md`

---
